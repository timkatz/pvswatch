name: Build and publish container

on:
  push:
    tags:
      # CalVer: YYYY.MM.DD or YYYY.MM.DD.N (multiple releases per day)
      - "20[0-9][0-9].[0-9]+.[0-9]+"
      - "20[0-9][0-9].[0-9]+.[0-9]+.[0-9]+"
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=ref,event=tag
            # Roll-up tag: 2026.04.25.3 → also tagged 2026.04.25
            type=match,pattern=^(\d{4}\.\d{2}\.\d{2})\.\d+$,group=1
            # Year roll-up: 2026.04.25.3 → also tagged 2026
            type=match,pattern=^(\d{4})\.\d{2}\.\d{2},group=1
            type=raw,value=latest,enable=${{ startsWith(github.ref, 'refs/tags/2') }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
